services:
  apisec:
    build:
      dockerfile: Dockerfile
    image: wwt92829/csc842-apisec
    ports:
      - "8080:80"   # Web server
      - "2222:22"   # SSH
    container_name: csc842-apisec
    restart: always
